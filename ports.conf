PORT_DBDIR= /usr/ports/options/${MACHINE_ARCH}

.if ${.CURDIR:M/usr/ports/devel/arm-none-eabi-*}
NO_CFLAGS_AND_CPUTYPE=
.endif

.if ${.CURDIR:M/usr/ports/emulators/qemu*}
NO_CFLAGS_AND_CPUTYPE=
.endif

.if ${.CURDIR:M/usr/ports/local/u-boot*} || ${.CURDIR:M/usr/ports/sysutils/u-boot*}
NO_CFLAGS_AND_CPUTYPE=
.endif

.if !defined(NO_CFLAGS_AND_CPUTYPE)
.  if ${MACHINE_ARCH} == "amd64"
CFLAGS= -O2 -fno-strict-aliasing -pipe -mmmx -msse -msse2 -msse3
# CFLAGS= -O2 -fno-strict-aliasing -pipe -mmmx -msse -msse2 -msse3 -mssse3 -msse4.1
# CFLAGS= -O2 -fno-strict-aliasing -pipe -mmmx -msse -msse2 -msse3 -mssse3 -msse4.1 -msse4.2
# CFLAGS= -O2 -fno-strict-aliasing -pipe -mmmx -msse -msse2 -msse3 -mssse3 -msse4.1 -msse4.2 -avx -avx2
.  endif
.endif

OPTIONS_UNSET+= DOCS EXAMPLES
OPTIONS_UNSET+= NLS

SAMBA4_PORT=net/samba46

.if ${.CURDIR:M/usr/ports/archivers/libarchive}
OPTIONS_SET+=   LZ4
OPTIONS_SET+=   LZO
OPTIONS_UNSET+= NETTLE
.endif

.if ${.CURDIR:M/usr/ports/archivers/archivers/unrar}
OPTIONS_SET+=   OPENSSL_AES
.endif

.if ${.CURDIR:M/usr/ports/archivers/unzip}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= ICONV
OPTIONS_UNSET+= CHINESE
OPTIONS_UNSET+= KOREAN
OPTIONS_UNSET+= RUSSIAN
.endif

.if ${.CURDIR:M/usr/ports/archivers/lzo2}
WITHOUT_CHECKS= yes
.endif

.if ${.CURDIR:M/usr/ports/audio/alsa-lib}
OPTIONS_UNSET+= PYTHON
.endif

.if ${.CURDIR:M/usr/ports/audio/alsa-plugins}
OPTIONS_UNSET+= FFMPEG
OPTIONS_UNSET+= JACK
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= SAMPLERATE
OPTIONS_UNSET+= SPEEX
OPTIONS_SET+=   IO_PTR
OPTIONS_UNSET+= BLKCNT_P2
OPTIONS_SET+=   BUFSZ_P2
OPTIONS_UNSET+= VERBOSE
.endif

.if ${.CURDIR:M/usr/ports/audio/audacity}
OPTIONS_UNSET+= ALSA
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= DYNLOAD
OPTIONS_UNSET+= FFMPEG
OPTIONS_SET+=   FLAC
OPTIONS_SET+=   ID3TAG
OPTIONS_UNSET+= LADSPA
OPTIONS_UNSET+= LAME
OPTIONS_UNSET+= LV2
OPTIONS_SET+=   MAD
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   SBSMS
OPTIONS_SET+=   SOUNDTOUCH
OPTIONS_SET+=   SSE
OPTIONS_UNSET+= TWOLAME
OPTIONS_SET+=   VAMP
OPTIONS_SET+=   VORBIS
OPTIONS_SET+=   VST
.endif

.if ${.CURDIR:M/usr/ports/audio/liba52}
OPTIONS_UNSET+= DJBFFT
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= OPTIMIZED_CFLAGS
.endif

.if ${.CURDIR:M/usr/ports/audio/libcanberra-gtk3}
OPTIONS_UNSET+= GSTREAMER
OPTIONS_UNSET+= PULSEAUDIO
.endif

.if ${.CURDIR:M/usr/ports/audio/libmad}
OPTIONS_UNSET+= SPEED
.endif

.if ${.CURDIR:M/usr/ports/audio/libsidplayfp}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= DOXYGEN
OPTIONS_SET+=   BHINTS
OPTIONS_SET+=   INLINE
OPTIONS_UNSET+= MMX
.endif

.if ${.CURDIR:M/usr/ports/audio/libsndfile}
OPTIONS_UNSET+= CPU_CLIP
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   EXTERNAL
.endif

.if ${.CURDIR:M/usr/ports/audio/libsoxr}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= OPENMP
OPTIONS_UNSET+= SIMD
OPTIONS_UNSET+= AVFFT
OPTIONS_UNSET+= PFFFT
.endif

.if ${.CURDIR:M/usr/ports/audio/mpg123}
OPTIONS_UNSET+= SNDIO
.endif

.if ${.CURDIR:M/usr/ports/audio/openal-soft}
OPTIONS_UNSET+= CONFIG
OPTIONS_UNSET+= FLUIDSYNTH
OPTIONS_UNSET+= JACK
OPTIONS_SET+=   OSS
OPTIONS_UNSET+= PORTAUDIO
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= SNDIO
.endif

.if ${.CURDIR:M/usr/ports/audio/opusfile}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= DOXYGEN
.endif

.if ${.CURDIR:M/usr/ports/audio/sdl_sound}
OPTIONS_SET+=   FLAC
OPTIONS_SET+=   MIDI
OPTIONS_UNSET+= MIKMOD
OPTIONS_UNSET+= MODPLUG
OPTIONS_UNSET+= PHYSFS
OPTIONS_UNSET+= SMPEG
OPTIONS_UNSET+= SPEEX
OPTIONS_SET+=   VORBIS
.endif

.if ${.CURDIR:M/usr/ports/audio/soundtouch}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= INTEGER_SAMPLES
OPTIONS_UNSET+= OPENMP
OPTIONS_SET+=   SSE
.endif

.if ${.CURDIR:M/usr/ports/audio/taglib}
OPTIONS_UNSET+= RCC
.endif

.if ${.CURDIR:M/usr/ports/audio/timidity}
OPTIONS_SET+=   GOEMON
OPTIONS_UNSET+= GUSPAT
.endif

.if ${.CURDIR:M/usr/ports/audio/wavpack}
OPTIONS_SET+=   ASM
.endif

.if ${.CURDIR:M/usr/ports/audio/wildmidi}
OPTIONS_SET+=   PLAYER
OPTIONS_UNSET+= PLAYERSTATIC
OPTIONS_UNSET+= STATIC
OPTIONS_SET+=   ALSA
OPTIONS_UNSET+= OPENAL
OPTIONS_UNSET+= OSS
.endif

.if ${.CURDIR:M/usr/ports/benchmarks/iperf}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   IPV6
OPTIONS_SET+=   THREADS
.endif

.if ${.CURDIR:M/usr/ports/converters/libiconv}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   ENCODINGS
OPTIONS_UNSET+= PATCHES
.endif

.if ${.CURDIR:M/usr/ports/databases/leveldb}
OPTIONS_UNSET+= GPERFTOOLS
OPTIONS_SET+=   SNAPPY
.endif

.if ${.CURDIR:M/usr/ports/databases/ldb}
OPTIONS_UNSET+= MANPAGES
.endif

.if ${.CURDIR:M/usr/ports/databases/sqlite3}
OPTIONS_UNSET+= ARMOR
OPTIONS_UNSET+= DBPAGE
OPTIONS_SET+=   DBSTAT
OPTIONS_UNSET+= DIRECT_READ
OPTIONS_UNSET+= EXTENSION
OPTIONS_UNSET+= FTS3_TOKEN
OPTIONS_SET+=   FTS4
OPTIONS_UNSET+= FTS5
OPTIONS_UNSET+= JSON1
OPTIONS_UNSET+= LIKENOTBLOB
OPTIONS_UNSET+= MEMMAN
OPTIONS_SET+=   METADATA
OPTIONS_UNSET+= NULL_TRIM
OPTIONS_UNSET+= RBU
OPTIONS_SET+=   SECURE_DELETE
OPTIONS_UNSET+= SESSION
OPTIONS_UNSET+= SOUNDEX
OPTIONS_UNSET+= STMT
OPTIONS_SET+=   STSHELL
OPTIONS_SET+=   THREADS
OPTIONS_UNSET+= UNKNOWN_SQL
OPTIONS_SET+=   UNLOCK_NOTIFY
OPTIONS_UNSET+= UPD_DEL_LIMIT
OPTIONS_UNSET+= URI
OPTIONS_UNSET+= URI_AUTHORITY
OPTIONS_UNSET+= ICU
OPTIONS_UNSET+= UNICODE61
OPTIONS_UNSET+= RTREE
OPTIONS_UNSET+= RTREE_INT
OPTIONS_UNSET+= READLINES
OPTIONS_UNSET+= READLINEP
OPTIONS_UNSET+= EDITLINE
OPTIONS_UNSET+= TS0
OPTIONS_SET+=   TS1
OPTIONS_UNSET+= TS2
OPTIONS_UNSET+= TS3
OPTIONS_UNSET+= STAT3
OPTIONS_UNSET+= STAT4
.endif

.if ${.CURDIR:M/usr/ports/databases/tdb}
OPTIONS_UNSET+= MANPAGES
.endif

.if ${.CURDIR:M/usr/ports/deskutils/parcellite}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   NLS
.endif

.if ${.CURDIR:M/usr/ports/deskutils/spice-gtk}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= SASL
OPTIONS_SET+=   GSTREAMER
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= NOAUDIO
.endif

.if ${.CURDIR:M/usr/ports/devel/apr1}
OPTIONS_SET+=   DEVRANDOM
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= BDB
OPTIONS_UNSET+= GDBM
OPTIONS_UNSET+= LDAP
OPTIONS_UNSET+= MYSQL
OPTIONS_UNSET+= NDBM
OPTIONS_UNSET+= ODBC
OPTIONS_UNSET+= PGSQL
OPTIONS_UNSET+= SQLITE
OPTIONS_UNSET+= FREETDS
OPTIONS_SET+=   SSL
OPTIONS_UNSET+= NSS
.endif

.if ${.CURDIR:M/usr/ports/devel/arm-none-eabi-binutils}
OPTIONS_UNSET+= RELRO
OPTIONS_UNSET+= STATIC
.endif

.if ${.CURDIR:M/usr/ports/devel/binutils}
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= RELRO
OPTIONS_UNSET+= STATIC
.endif

.if ${.CURDIR:M/usr/ports/devel/boehm-gc}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
.endif

.if ${.CURDIR:M/usr/ports/devel/boehm-gc-threaded}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= PARALLEL_MARK
.endif

.if ${.CURDIR:M/usr/ports/devel/boost-libs}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= OPTIMIZED_CFLAGS
OPTIONS_SET+=   ICONV
OPTIONS_SET+=   ICU
.endif

.if ${.CURDIR:M/usr/ports/devel/cargo}
OPTIONS_SET+=   BOOTSTRAP
.endif

.if ${.CURDIR:M/usr/ports/devel/cmake}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   MANPAGES
.endif

.if ${.CURDIR:M/usr/ports/devel/cppunit}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= DOXYGEN
.endif

.if ${.CURDIR:M/usr/ports/devel/cscope}
OPTIONS_UNSET+= XCSCOPE
.endif

.if ${.CURDIR:M/usr/ports/devel/dbus}
OPTIONS_UNSET+= MANPAGES
OPTIONS_SET+=   X11
.endif

.if ${.CURDIR:M/usr/ports/devel/gconf2}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= LDAP
.endif

.if ${.CURDIR:M/usr/ports/devel/gdb}
OPTIONS_UNSET+= DEBUG
OPTIONS_SET+=   GDB_LINK
OPTIONS_UNSET+= GUILE
OPTIONS_SET+=   KGDB
OPTIONS_UNSET+= PYTHON
OPTIONS_SET+=   TUI
OPTIONS_UNSET+= BASE_READLINE
OPTIONS_UNSET+= BUNDLED_READLINE
OPTIONS_SET+=   PORT_READLINE
.endif

.if ${.CURDIR:M/usr/ports/devel/geany}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= VTE
.endif

.if ${.CURDIR:M/usr/ports/devel/gettext-tools}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   THREADS
.endif

.if ${.CURDIR:M/usr/ports/devel/git}
OPTIONS_UNSET+= CONTRIB
OPTIONS_SET+=   CURL
OPTIONS_UNSET+= CVS
OPTIONS_UNSET+= GITWEB
OPTIONS_UNSET+= GUI
OPTIONS_UNSET+= HTMLDOCS
OPTIONS_SET+=   ICONV
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= P4
OPTIONS_SET+=   PCRE
OPTIONS_SET+=   PERL
OPTIONS_UNSET+= SEND_EMAIL
OPTIONS_UNSET+= SUBTREE
OPTIONS_UNSET+= SVN
.endif

.if ${.CURDIR:M/usr/ports/devel/glib20}
OPTIONS_UNSET+= COLLATION_FIX
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= GDB
.endif

.if ${.CURDIR:M/usr/ports/devel/icu}
OPTIONS_SET+=   THREADS
.endif

.if ${.CURDIR:M/usr/ports/devel/kf5-extra-cmake-modules}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= HELP
OPTIONS_UNSET+= MANPAGES
OPTIONS_UNSET+= TEST
.endif

.if ${.CURDIR:M/usr/ports/devel/libdevq}
OPTIONS_UNSET+= PROGRAMS
.endif

.if ${.CURDIR:M/usr/ports/devel/libevent}
OPTIONS_SET+=   OPENSSL
OPTIONS_SET+=   THREADS
.endif

.if ${.CURDIR:M/usr/ports/devel/libffi}
OPTIONS_UNSET+= TESTS
.endif

.if ${.CURDIR:M/usr/ports/devel/libglade2}
OPTIONS_SET+=   PYTHON
.endif

.if ${.CURDIR:M/usr/ports/devel/libvirt}
OPTIONS_SET+=   BHYVE
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= GNUTLS
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= NSS
OPTIONS_SET+=   QEMU
OPTIONS_SET+=   READLINE
OPTIONS_UNSET+= SASL
OPTIONS_UNSET+= XEN
OPTIONS_UNSET+= ZFS
.endif

.if ${.CURDIR:M/usr/ports/devel/libvirt-glib}
OPTIONS_UNSET+= VALA
.endif

.if ${.CURDIR:M/usr/ports/devel/llvm40}
OPTIONS_SET+=   CLANG
OPTIONS_SET+=   COMPILER_RT
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   EXTRAS
OPTIONS_SET+=   GOLD
OPTIONS_SET+=   LIT
OPTIONS_SET+=   LLD
OPTIONS_SET+=   LLDB
OPTIONS_SET+=   OPENMP
.endif

.if ${.CURDIR:M/usr/ports/devel/m4}
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= LIBSIGSEGV
.endif

.if ${.CURDIR:M/usr/ports/devel/mercurial}
OPTIONS_UNSET+= CA_BUNDLE
OPTIONS_SET+=   DATA
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= FREEBSD
OPTIONS_UNSET+= NLS
.endif

.if ${.CURDIR:M/usr/ports/devel/nasm}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   RDOFF
.endif

.if ${.CURDIR:M/usr/ports/devel/nspr}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= TEST
.endif

.if ${.CURDIR:M/usr/ports/devel/pcre}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   MAN3
OPTIONS_SET+=   STACK_RECURSION
OPTIONS_UNSET+= LIBEDIT
OPTIONS_UNSET+= READLINE
.endif

.if ${.CURDIR:M/usr/ports/devel/pcre2}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   STACK_RECURSION
OPTIONS_UNSET+= LIBEDIT
OPTIONS_UNSET+= READLINE
.endif

.if ${.CURDIR:M/usr/ports/devel/pydbus-common}
OPTIONS_UNSET+= HTMLDOCS
.endif

.if ${.CURDIR:M/usr/ports/devel/py-Jinja2}
OPTIONS_SET+=   BABEL
OPTIONS_UNSET+= EXAMPLES
.endif

.if ${.CURDIR:M/usr/ports/devel/readline}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   TERMCAP
.endif

.if ${.CURDIR:M/usr/ports/devel/sdl12}
OPTIONS_UNSET+= AALIB
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_SET+=   OPENGL
OPTIONS_UNSET+= SVGALIB
OPTIONS_SET+=   VGL
OPTIONS_SET+=   X11
OPTIONS_UNSET+= ESOUND
OPTIONS_UNSET+= NAS
OPTIONS_SET+=   OSS
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= SNDIO
.endif

.if ${.CURDIR:M/usr/ports/devel/sdl20}
OPTIONS_UNSET+= ALSA
OPTIONS_SET+=   ASM
OPTIONS_SET+=   DLOPEN
OPTIONS_UNSET+= ESOUND
OPTIONS_UNSET+= JACK
OPTIONS_UNSET+= NAS
OPTIONS_SET+=   OSS
OPTIONS_SET+=   PTHREADS
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= SAMPLERATE
OPTIONS_SET+=   SDL_ATOMIC
OPTIONS_SET+=   SDL_AUDIO
OPTIONS_SET+=   SDL_CPUINFO
OPTIONS_SET+=   SDL_EVENTS
OPTIONS_SET+=   SDL_FILE
OPTIONS_SET+=   SDL_HAPTIC
OPTIONS_SET+=   SDL_JOYSTICK
OPTIONS_SET+=   SDL_LOADSO
OPTIONS_SET+=   SDL_POWER
OPTIONS_SET+=   SDL_RENDER
OPTIONS_SET+=   SDL_THREADS
OPTIONS_SET+=   SDL_TIMERS
OPTIONS_SET+=   SDL_VIDEO
OPTIONS_UNSET+= SNDIO
OPTIONS_SET+=   VIDEO_KMSDRM
OPTIONS_SET+=   VIDEO_OPENGL
OPTIONS_SET+=   VIDEO_X11
.endif

.if ${.CURDIR:M/usr/ports/devel/subversion}
OPTIONS_UNSET+= BDB
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   FREEBSD_TEMPLATE
OPTIONS_UNSET+= MAINTAINER_DEBUG
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= SASL
OPTIONS_SET+=   SERF
OPTIONS_UNSET+= STATIC
OPTIONS_UNSET+= SVNSERVE_WRAPPER
OPTIONS_UNSET+= TEST
OPTIONS_UNSET+= TOOLS
.endif

.if ${.CURDIR:M/usr/ports/devel/swig13}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= LUA
.endif

.if ${.CURDIR:M/usr/ports/devel/talloc}
OPTIONS_UNSET+= MANPAGES
.endif

.if ${.CURDIR:M/usr/ports/devel/tevent}
OPTIONS_UNSET+= MANPAGES
.endif

.if ${.CURDIR:M/usr/ports/dns/dnsmasq}
OPTIONS_UNSET+= DBUS
OPTIONS_UNSET+= DNSSEC
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= IPSET
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= LUA
OPTIONS_UNSET+= IDN
OPTIONS_UNSET+= NLS
WITH_DEBUG= yes
.endif

.if ${.CURDIR:M/usr/ports/dns/py-dnspython}
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= PYCRYPTO
.endif

.if ${.CURDIR:M/usr/ports/editors/libreoffice}
OPTIONS_SET+=   CUPS
OPTIONS_UNSET+= GNOME
OPTIONS_SET+=   GTK2
OPTIONS_UNSET+= GTK3
OPTIONS_UNSET+= JAVA
OPTIONS_UNSET+= KDE4
OPTIONS_UNSET+= MMEDIA
OPTIONS_UNSET+= PGSQL
OPTIONS_UNSET+= SDK
OPTIONS_UNSET+= SYSTRAY
OPTIONS_UNSET+= TEST
OPTIONS_UNSET+= VERBOSE
OPTIONS_UNSET+= WEBDAV
.endif

.if ${.CURDIR:M/usr/ports/emulators/dosbox}
OPTIONS_UNSET+= DEBUGGER_ENABLE
OPTIONS_UNSET+= DEBUGGER_HEAVY
.endif

.if ${.CURDIR:M/usr/ports/emulators/qemu}
OPTIONS_SET+=   CDROM_DMA
OPTIONS_SET+=   CURL
OPTIONS_SET+=   DOCS
OPTIONS_SET+=   GNS3
OPTIONS_UNSET+= GNUTLS
OPTIONS_SET+=   GTK2
OPTIONS_SET+=   JPEG
OPTIONS_SET+=   NCURSES
OPTIONS_SET+=   OPENGL
OPTIONS_SET+=   PCAP
OPTIONS_SET+=   PNG
OPTIONS_UNSET+= SAMBA
OPTIONS_UNSET+= SASL
OPTIONS_UNSET+= STATIC_LINK
OPTIONS_UNSET+= USBREDIR
OPTIONS_SET+=   VDE
OPTIONS_SET+=   X11
OPTIONS_UNSET+= X86_TARGETS
.endif

.if ${.CURDIR:M/usr/ports/emulators/virtualbox-ose-additions}
OPTIONS_SET+=   DBUS
OPTIONS_UNSET+= DEBUG
OPTIONS_SET+=   OPENGL
OPTIONS_SET+=   X11
.endif

.if ${.CURDIR:M/usr/ports/ftp/curl}
OPTIONS_SET+=   CA_BUNDLE
OPTIONS_SET+=   COOKIES
OPTIONS_UNSET+= CURL_DEBUG
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= HTTP2
OPTIONS_UNSET+= IDN
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= LDAP
OPTIONS_UNSET+= LDAPS
OPTIONS_UNSET+= LIBSSH2
OPTIONS_UNSET+= METALINK
OPTIONS_SET+=   PROXY
OPTIONS_UNSET+= PSL
OPTIONS_UNSET+= RTMP
OPTIONS_UNSET+= SMB
OPTIONS_SET+=   TLS_SRP
OPTIONS_UNSET+= GSSAPI_BASE
OPTIONS_UNSET+= GSSAPI_HEIMDAL
OPTIONS_UNSET+= GSSAPI_MIT
OPTIONS_SET+=   GSSAPI_NONE
OPTIONS_UNSET+= CARES
OPTIONS_SET+=   THREADED_RESOLVER
OPTIONS_UNSET+= GNUTLS
OPTIONS_UNSET+= NSS
OPTIONS_SET+=   OPENSSL
OPTIONS_UNSET+= POLARSSL
OPTIONS_UNSET+= WOLFSSL
.endif

.if ${.CURDIR:M/usr/ports/ftp/wget}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= IDN
OPTIONS_SET+=   IPV6
OPTIONS_SET+=   MANPAGES
OPTIONS_UNSET+= METALINK
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= PCRE
OPTIONS_UNSET+= PSL
OPTIONS_UNSET+= GNUTLS
OPTIONS_SET+=   OPENSSL
.endif

.if ${.CURDIR:M/usr/ports/graphics/blender}
OPTIONS_SET+=   AVI
OPTIONS_SET+=   BULLET
OPTIONS_UNSET+= CAMERATRACK
OPTIONS_UNSET+= CINEON
OPTIONS_UNSET+= COLLADA
OPTIONS_SET+=   COMPOSITOR
OPTIONS_UNSET+= CYCLES
OPTIONS_UNSET+= CYCLESOSL
OPTIONS_UNSET+= DDS
OPTIONS_UNSET+= FFMPEG
OPTIONS_SET+=   FFTW3
OPTIONS_UNSET+= FRAMESERVER
OPTIONS_SET+=   FREESTYLE
OPTIONS_SET+=   GAMEENGINE
OPTIONS_UNSET+= HDR
OPTIONS_UNSET+= HEADLESS
OPTIONS_UNSET+= INPUT_NDOF
OPTIONS_UNSET+= JACK
OPTIONS_SET+=   LIBMV
OPTIONS_SET+=   LZMA
OPTIONS_SET+=   LZO
OPTIONS_UNSET+= MENU
OPTIONS_SET+=   MOD_BOOLEAN
OPTIONS_SET+=   MOD_FLUID
OPTIONS_UNSET+= MOD_OCEANSIM
OPTIONS_SET+=   MOD_REMESH
OPTIONS_SET+=   MOD_SMOKE
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   OPENAL
OPTIONS_SET+=   OPENCOLORIO
OPTIONS_SET+=   OPENEXR
OPTIONS_SET+=   OPENIMAGEIO
OPTIONS_SET+=   OPENJPEG
OPTIONS_SET+=   OPENSUBDIV
OPTIONS_SET+=   PLAYER
OPTIONS_UNSET+= RAYOPTIMIZATION
OPTIONS_UNSET+= REDCODE
OPTIONS_SET+=   SDL
OPTIONS_UNSET+= SNDFILE
OPTIONS_SET+=   TIFF
OPTIONS_SET+=   XF86VMODE
OPTIONS_SET+=   XINPUT
.endif

.if ${.CURDIR:M/usr/ports/graphics/cairo}
OPTIONS_SET+=   GLIB
OPTIONS_UNSET+= OPENGL
OPTIONS_SET+=   X11
OPTIONS_SET+=   XCB
.endif

.if ${.CURDIR:M/usr/ports/graphics/colord}
OPTIONS_UNSET+= MANPAGES
OPTIONS_UNSET+= PPROFILES
.endif

.if ${.CURDIR:M/usr/ports/graphics/dri}
OPTIONS_SET+=   TEXTURE
OPTIONS_UNSET+= VAAPI
OPTIONS_UNSET+= VDPAU
.endif

.if ${.CURDIR:M/usr/ports/graphics/epdfview}
OPTIONS_UNSET+= CUPS
OPTIONS_UNSET+= NLS
.endif

.if ${.CURDIR:M/usr/ports/graphics/gdk-pixbuf2}
OPTIONS_SET+=   JASPER
OPTIONS_SET+=   JPEG
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   PNG
OPTIONS_SET+=   TIFF
.endif

.if ${.CURDIR:M/usr/ports/graphics/gegl}
OPTIONS_SET+=   CAIRO
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   ENSCRIPT
OPTIONS_UNSET+= EXAMPLES
OPTIONS_SET+=   EXIV2
OPTIONS_UNSET+= FFMPEG
OPTIONS_UNSET+= GRAPHVIZ
OPTIONS_UNSET+= JASPER
OPTIONS_SET+=   JPEG
OPTIONS_SET+=   LIBRSVG2
OPTIONS_UNSET+= LUA
OPTIONS_UNSET+= OPENEXR
OPTIONS_SET+=   OPENRAW
OPTIONS_SET+=   PANGO
OPTIONS_SET+=   PIXBUF
OPTIONS_SET+=   PNG
OPTIONS_SET+=   SDL
OPTIONS_UNSET+= SIMD
OPTIONS_SET+=   SPIRO
OPTIONS_SET+=   V4L
.endif

.if ${.CURDIR:M/usr/ports/graphics/giflib}
OPTIONS_UNSET+= DOCBOOK
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= X11
.endif

.if ${.CURDIR:M/usr/ports/graphics/gimp}
OPTIONS_UNSET+= HELP
OPTIONS_UNSET+= PRINT
OPTIONS_UNSET+= PYTHON
.endif

.if ${.CURDIR:M/usr/ports/graphics/gimp-app}
OPTIONS_UNSET+= AA
OPTIONS_SET+=   DBUS
OPTIONS_UNSET+= GHOSTSCRIPT
OPTIONS_UNSET+= GVFS
OPTIONS_UNSET+= HELPBROWSER
OPTIONS_UNSET+= JASPER
OPTIONS_SET+=   LIBEXIF
OPTIONS_UNSET+= LIBMNG
OPTIONS_SET+=   LIBRSVG2
OPTIONS_UNSET+= POPPLER
OPTIONS_UNSET+= SIMD
OPTIONS_UNSET+= WMF
.endif

.if ${.CURDIR:M/usr/ports/graphics/ImageMagick}
OPTIONS_SET+=   16BIT_PIXEL
OPTIONS_SET+=   BZIP2
OPTIONS_UNSET+= DJVU
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= FFTW
OPTIONS_SET+=   FONTCONFIG
OPTIONS_UNSET+= FPX
OPTIONS_UNSET+= FREETYPE
OPTIONS_UNSET+= GRAPHVIZ
OPTIONS_UNSET+= GSLIB
OPTIONS_UNSET+= HDRI
OPTIONS_SET+=   JBIG
OPTIONS_SET+=   JPEG
OPTIONS_SET+=   JPEG2000
OPTIONS_SET+=   LCMS2
OPTIONS_SET+=   LQR
OPTIONS_SET+=   LZMA
OPTIONS_UNSET+= MODULES
OPTIONS_UNSET+= OPENEXR
OPTIONS_UNSET+= OPENMP
OPTIONS_UNSET+= PANGO
OPTIONS_UNSET+= PDF
OPTIONS_SET+=   PERL
OPTIONS_SET+=   PNG
OPTIONS_SET+=   RAW
OPTIONS_UNSET+= SVG
OPTIONS_UNSET+= TESTS
OPTIONS_UNSET+= THREADS
OPTIONS_SET+=   TIFF
OPTIONS_SET+=   WEBP
OPTIONS_UNSET+= WMF
OPTIONS_UNSET+= X11
.endif

.if ${.CURDIR:M/usr/ports/graphics/imlib2}
OPTIONS_UNSET+= GIF
OPTIONS_UNSET+= ID3
OPTIONS_SET+=   JPEG
OPTIONS_SET+=   PNG
OPTIONS_SET+=   TIFF
OPTIONS_SET+=   X11
.endif

.if ${.CURDIR:M/usr/ports/graphics/inkscape}
OPTIONS_UNSET+= CDR
OPTIONS_UNSET+= DBUS
OPTIONS_SET+=   POPPLER
OPTIONS_UNSET+= VISIO
OPTIONS_UNSET+= WPG
.endif

.if ${.CURDIR:M/usr/ports/graphics/jasper}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= OPENGL
OPTIONS_UNSET+= UUID
.endif

.if ${.CURDIR:M/usr/ports/graphics/lcms}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   JPEGICC
OPTIONS_SET+=   TIFFICC
.endif

.if ${.CURDIR:M/usr/ports/graphics/lcms2}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= JPEGICC
OPTIONS_UNSET+= TIFFICC
.endif

.if ${.CURDIR:M/usr/ports/graphics/lensfun}
OPTIONS_SET+=   PYHELPERS
OPTIONS_SET+=   SIMD
.endif

.if ${.CURDIR:M/usr/ports/graphics/libdrm}
OPTIONS_UNSET+= MANPAGES
OPTIONS_UNSET+= TESTS
.endif

.if ${.CURDIR:M/usr/ports/graphics/libepoxy}
OPTIONS_SET+=   EGL
.endif

.if ${.CURDIR:M/usr/ports/graphics/libetonyek01}
OPTIONS_UNSET+= TEST
.endif

.if ${.CURDIR:M/usr/ports/graphics/liblqr-1}
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= HTMLDOCS
.endif

.if ${.CURDIR:M/usr/ports/graphics/libmng}
OPTIONS_UNSET+= MNG_OPTIMIZE
.endif

.if ${.CURDIR:M/usr/ports/graphics/libopenraw}
OPTIONS_UNSET+= GNOME
OPTIONS_UNSET+= TEST
.endif

.if ${.CURDIR:M/usr/ports/graphics/libpotrace}
OPTIONS_SET+=   A4
OPTIONS_SET+=   METRIC
.endif

.if ${.CURDIR:M/usr/ports/graphics/libraw}
OPTIONS_UNSET+= DEMOSAIC_PACK_GPL2
OPTIONS_UNSET+= DEMOSAIC_PACK_GPL3
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= JASPER
OPTIONS_SET+=   JPEG
OPTIONS_SET+=   LCMS2
OPTIONS_UNSET+= OPENMP
.endif

.if ${.CURDIR:M/usr/ports/graphics/mesa-dri}
OPTIONS_SET+=   TEXTURE
OPTIONS_UNSET+= VAAPI
OPTIONS_UNSET+= VDPAU
.endif

.if ${.CURDIR:M/usr/ports/graphics/mesa-libs}
OPTIONS_UNSET+= WAYLAND
.endif

.if ${.CURDIR:M/usr/ports/graphics/OpenEXR}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= LARGE_STACK
.endif

.if ${.CURDIR:M/usr/ports/graphics/openimageio}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= FFMPEG
OPTIONS_SET+=   FREETYPE
OPTIONS_UNSET+= GIF
OPTIONS_UNSET+= IMAGEVIEWER
OPTIONS_SET+=   OCIO
OPTIONS_UNSET+= OPENCV
OPTIONS_SET+=   OPENJPEG
OPTIONS_UNSET+= RAW
OPTIONS_UNSET+= TEST
.endif

.if ${.CURDIR:M/usr/ports/graphics/opensubdiv}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= OPENCL
OPTIONS_UNSET+= PTEX
OPTIONS_SET+=   TBB
OPTIONS_UNSET+= TEST
OPTIONS_UNSET+= TUTORIALS
.endif

.if ${.CURDIR:M/usr/ports/graphics/png}
OPTIONS_SET+=   APNG
OPTIONS_UNSET+= PNGTEST
.endif

.if ${.CURDIR:M/usr/ports/graphics/poppler}
OPTIONS_SET+=   CAIRO
OPTIONS_UNSET+= CURL
OPTIONS_SET+=   OPENJPEG
.endif

.if ${.CURDIR:M/usr/ports/graphics/rawtherapee}
OPTIONS_UNSET+= NATIVE
OPTIONS_SET+=   OPENMP
OPTIONS_SET+=   OPTIMIZED_CFLAGS
.endif

.if ${.CURDIR:M/usr/ports/graphics/sane-backends}
OPTIONS_UNSET+= AVAHI
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= GPHOTO
OPTIONS_UNSET+= IEEE1284
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= SNMP
OPTIONS_SET+=   USB
OPTIONS_UNSET+= V4L
.endif

.if ${.CURDIR:M/usr/ports/graphics/vigra}
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   FFTW
OPTIONS_UNSET+= HDF5
OPTIONS_UNSET+= NUMPY
OPTIONS_UNSET+= OPENEXR
OPTIONS_SET+=   PNG
OPTIONS_SET+=   JPEG
OPTIONS_SET+=   TIFF
.endif

.if ${.CURDIR:M/usr/ports/graphics/webp}
OPTIONS_UNSET+= X11
.endif

.if ${.CURDIR:M/usr/ports/lang/gcc6}
OPTIONS_SET+=   BOOTSTRAP
OPTIONS_UNSET+= GRAPHITE
OPTIONS_UNSET+= JAVA
.endif

.if ${.CURDIR:M/usr/ports/lang/guile2}
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   THREADS
.endif

.if ${.CURDIR:M/usr/ports/lang/perl5.24}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DTRACE
OPTIONS_UNSET+= GDBM
OPTIONS_SET+=   MULTIPLICITY
OPTIONS_SET+=   PERL_64BITINT
OPTIONS_SET+=   PTHREAD
OPTIONS_UNSET+= SITECUSTOMIZE
OPTIONS_SET+=   THREADS
OPTIONS_UNSET+= PERL_MALLOC
.endif

.if ${.CURDIR:M/usr/ports/lang/python27}
OPTIONS_UNSET+= DEBUG
OPTIONS_SET+=   IPV6
OPTIONS_SET+=   LIBFFI
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   PYMALLOC
OPTIONS_SET+=   SEM
OPTIONS_SET+=   THREADS
OPTIONS_UNSET+= UCS2
OPTIONS_SET+=   UCS4
.endif

.if ${.CURDIR:M/usr/ports/lang/python35}
OPTIONS_UNSET+= DEBUG
OPTIONS_SET+=   IPV6
OPTIONS_SET+=   LIBFFI
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   PYMALLOC
OPTIONS_SET+=   THREADS
OPTIONS_UNSET+= FNV
OPTIONS_UNSET+= SIPHASH
.endif

.if ${.CURDIR:M/usr/ports/lang/python36}
OPTIONS_UNSET+= DEBUG
OPTIONS_SET+=   IPV6
OPTIONS_SET+=   LIBFFI
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   PYMALLOC
OPTIONS_SET+=   THREADS
OPTIONS_UNSET+= FNV
OPTIONS_UNSET+= SIPHASH
.endif

.if ${.CURDIR:M/usr/ports/lang/ruby23}
OPTIONS_UNSET+= CAPIDOCS
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= GMP
OPTIONS_UNSET+= RDOC
OPTIONS_SET+=   LIBEDIT
OPTIONS_UNSET+= READLINE
.endif

.if ${.CURDIR:M/usr/ports/lang/rust}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= GDB
OPTIONS_UNSET+= LLNEXTGEN
OPTIONS_UNSET+= PORT_LLVM
OPTIONS_UNSET+= SOURCES
.endif

.if ${.CURDIR:M/usr/ports/lang/spidermonkey170}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DTRACE
OPTIONS_UNSET+= GCZEAL
OPTIONS_UNSET+= JEMALLOC
OPTIONS_SET+=   METHODJIT
OPTIONS_SET+=   OPTIMIZE
OPTIONS_SET+=   READLINE
OPTIONS_SET+=   THREADSAFE
OPTIONS_SET+=   TRACEJIT
OPTIONS_SET+=   UTF8
.endif

.if ${.CURDIR:M/usr/ports/mail/thunderbird}
OPTIONS_SET+=   BUNDLED_CAIRO
OPTIONS_SET+=   CANBERRA
OPTIONS_SET+=   DBUS
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DTRACE
OPTIONS_UNSET+= FFMPEG
OPTIONS_UNSET+= GCONF
OPTIONS_UNSET+= GNOMEUI
OPTIONS_UNSET+= INTEGER_SAMPLES
OPTIONS_UNSET+= LIBPROXY
OPTIONS_SET+=   LIGHTNING
OPTIONS_SET+=   OPTIMIZED_CFLAGS
OPTIONS_UNSET+= PGO
OPTIONS_UNSET+= PROFILE
OPTIONS_UNSET+= RUST
OPTIONS_UNSET+= TEST
OPTIONS_SET+=   ALSA
OPTIONS_UNSET+= JACK
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= SNDIO
OPTIONS_UNSET+= GTK2
OPTIONS_SET+=   GTK3
.endif

.if ${.CURDIR:M/usr/ports/math/coinmp}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= GLPK
.endif

.if ${.CURDIR:M/usr/ports/math/fftw3}
OPTIONS_UNSET+= G77_WRAPPERS
OPTIONS_UNSET+= OPENMP
OPTIONS_UNSET+= OPENMPI
OPTIONS_SET+=   OPTIMIZED_CFLAGS
OPTIONS_SET+=   SIMD
.endif

.if ${.CURDIR:M/usr/ports/math/fftw3-float}
OPTIONS_UNSET+= G77_WRAPPERS
OPTIONS_UNSET+= OPENMP
OPTIONS_UNSET+= OPENMPI
OPTIONS_SET+=   OPTIMIZED_CFLAGS
OPTIONS_SET+=   SIMD
.endif

.if ${.CURDIR:M/usr/ports/math/galculator}
OPTIONS_UNSET+= GTK2
OPTIONS_SET+=   GTK3
.endif

.if ${.CURDIR:M/usr/ports/math/gmp}
OPTIONS_UNSET+= CPU_OPTS
.endif

.if ${.CURDIR:M/usr/ports/multimedia/avidemux}
OPTIONS_UNSET+= AMR
OPTIONS_UNSET+= CLI
OPTIONS_UNSET+= ESOUND
OPTIONS_SET+=   FAAC
OPTIONS_SET+=   FAAD
OPTIONS_UNSET+= FONTCONFIG
OPTIONS_UNSET+= FREETYPE
OPTIONS_UNSET+= FRIBIDI
OPTIONS_UNSET+= JACK
OPTIONS_UNSET+= LAME
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= OPUS
OPTIONS_UNSET+= OSS
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_SET+=   QT4
OPTIONS_SET+=   SDL
OPTIONS_UNSET+= TWOLAME
OPTIONS_UNSET+= VAAPI
OPTIONS_SET+=   VDPAU
OPTIONS_SET+=   VORBIS
OPTIONS_SET+=   VPX
OPTIONS_SET+=   X264
OPTIONS_UNSET+= X265
OPTIONS_SET+=   XVID
OPTIONS_SET+=   XVIDEO
.endif

.if ${.CURDIR:M/usr/ports/multimedia/ffmpeg}
OPTIONS_UNSET+= ALSA
OPTIONS_UNSET+= AMR_NB
OPTIONS_UNSET+= AMR_WB
OPTIONS_UNSET+= ASS
OPTIONS_UNSET+= BS2B
OPTIONS_UNSET+= CACA
OPTIONS_UNSET+= CDIO
OPTIONS_UNSET+= CELT
OPTIONS_UNSET+= CHROMAPRINT
OPTIONS_UNSET+= DC1394
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= DRM
OPTIONS_SET+=   FDK_AAC
OPTIONS_UNSET+= FFSERVER
OPTIONS_UNSET+= FLITE
OPTIONS_SET+=   FONTCONFIG
OPTIONS_SET+=   FREETYPE
OPTIONS_UNSET+= FREI0R
OPTIONS_UNSET+= FRIBIDI
OPTIONS_UNSET+= GME
OPTIONS_UNSET+= GSM
OPTIONS_SET+=   ICONV
OPTIONS_UNSET+= ILBC
OPTIONS_UNSET+= JACK
OPTIONS_UNSET+= KVAZAAR
OPTIONS_UNSET+= LADSPA
OPTIONS_UNSET+= LAME
OPTIONS_SET+=   LIBBLURAY
OPTIONS_UNSET+= LIBRSVG2
OPTIONS_UNSET+= LIBXML2
.  if ${MACHINE_ARCH} == "amd64"
OPTIONS_SET+=   MMX
.  endif
OPTIONS_UNSET+= MODPLUG
.  if ${MACHINE_ARCH} == "armv7"
OPTIONS_SET+=   NEON
.  endif
OPTIONS_UNSET+= OPENAL
OPTIONS_UNSET+= OPENCL
OPTIONS_UNSET+= OPENCV
OPTIONS_UNSET+= OPENGL
OPTIONS_UNSET+= OPENH264
OPTIONS_UNSET+= OPENJPEG
OPTIONS_SET+=   OPTIMIZED_CFLAGS
OPTIONS_SET+=   OPUS
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_SET+=   RTCPU
OPTIONS_UNSET+= RUBBERBAND
OPTIONS_UNSET+= SDL
OPTIONS_UNSET+= SMB
OPTIONS_UNSET+= SNAPPY
OPTIONS_UNSET+= SNDIO
OPTIONS_UNSET+= SOXR
OPTIONS_UNSET+= SPEEX
.  if ${MACHINE_ARCH} == "amd64"
OPTIONS_SET+=   SSE
.  endif
OPTIONS_UNSET+= SSH
OPTIONS_UNSET+= TESSERACT
OPTIONS_UNSET+= THEORA
OPTIONS_UNSET+= TWOLAME
OPTIONS_UNSET+= V4L
.  if ${MACHINE_ARCH} == "amd64"
OPTIONS_SET+=   VAAPI
OPTIONS_SET+=   VDPAU
.  elif ${MACHINE_ARCH} == "armv7"
OPTIONS_UNSET+= VAAPI
OPTIONS_UNSET+= VDPAU
.  endif
.  if ${MACHINE_ARCH} == "armv7"
  OPTIONS_SET+=   VFP
.  endif
OPTIONS_UNSET+= VIDSTAB
OPTIONS_UNSET+= VORBIS
OPTIONS_UNSET+= VO_AMRWBENC
OPTIONS_SET+=   VPX
OPTIONS_UNSET+= WAVPACK
OPTIONS_UNSET+= WEBP
OPTIONS_SET+=   X264
OPTIONS_SET+=   X265
OPTIONS_UNSET+= XCB
OPTIONS_UNSET+= XVID
OPTIONS_UNSET+= XVIDEO
OPTIONS_UNSET+= ZIMG
OPTIONS_UNSET+= ZMQ
OPTIONS_UNSET+= ZVBI
OPTIONS_SET+=   GPL3
OPTIONS_SET+=   NONFREE
OPTIONS_UNSET+= GCRYPT
OPTIONS_SET+=   GMP
OPTIONS_UNSET+= LIBRTMP
OPTIONS_SET+=   GNUTLS
OPTIONS_UNSET+= OPENSSL
.endif

.if ${.CURDIR:M/usr/ports/multimedia/gmtk}
OPTIONS_UNSET+= ALSA
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= GCONF
OPTIONS_UNSET+= GIO
OPTIONS_UNSET+= GTK3
OPTIONS_SET+=   NLS
OPTIONS_UNSET+= PULSEAUDIO
.endif

.if ${.CURDIR:M/usr/ports/multimedia/gstreamer}
OPTIONS_UNSET+= LIBCHECK
OPTIONS_UNSET+= NLS
.endif

.if ${.CURDIR:M/usr/ports/multimedia/gstreamer1}
OPTIONS_UNSET+= LIBCHECK
.endif

.if ${.CURDIR:M/usr/ports/multimedia/gstreamer1-plugins-core}
OPTIONS_SET+=   DVD
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libass}
OPTIONS_SET+=   ASM
OPTIONS_SET+=   HARFBUZZ
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libbluray}
OPTIONS_UNSET+= JAVA
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libdvdnav}
OPTIONS_UNSET+= DEBUG
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libdvdread}
OPTIONS_UNSET+= DVDCSS
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libmediainfo}
OPTIONS_UNSET+= CURL
OPTIONS_UNSET+= MMS
OPTIONS_SET+=   TINYXML2
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libva}
OPTIONS_UNSET+= DEBUG
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libvdpau}
OPTIONS_UNSET+= DEBUG
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libvpx}
OPTIONS_UNSET+= DEBUG
OPTIONS_SET+=   MULTIRES
OPTIONS_SET+=   POSTPROC
OPTIONS_SET+=   RTCPU
OPTIONS_SET+=   SHARED
OPTIONS_SET+=   SIZE_LIMIT
OPTIONS_UNSET+= TEST
OPTIONS_SET+=   THREADS
.endif

.if ${.CURDIR:M/usr/ports/multimedia/libx264}
OPTIONS_SET+=   ASM
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= GCC
OPTIONS_UNSET+= HI10P
OPTIONS_UNSET+= OPENCL
OPTIONS_UNSET+= PGO
.endif

.if ${.CURDIR:M/usr/ports/multimedia/mplayer}
OPTIONS_UNSET+= AALIB
OPTIONS_UNSET+= AMR_NB
OPTIONS_UNSET+= AMR_WB
OPTIONS_SET+=   ASS
OPTIONS_SET+=   BLURAY
OPTIONS_UNSET+= CACA
OPTIONS_UNSET+= CDIO
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DV
OPTIONS_SET+=   DVDNAV
OPTIONS_UNSET+= ENCA
OPTIONS_SET+=   FONTCONFIG
OPTIONS_UNSET+= FRIBIDI
OPTIONS_UNSET+= GIF
OPTIONS_UNSET+= GNUTLS
OPTIONS_UNSET+= GSM
OPTIONS_UNSET+= GUI
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= JACK
OPTIONS_UNSET+= LADSPA
OPTIONS_UNSET+= LIBMNG
OPTIONS_UNSET+= LIRC
OPTIONS_UNSET+= LZO
OPTIONS_UNSET+= NAS
OPTIONS_UNSET+= OPENAL
OPTIONS_SET+=   OPENGL
OPTIONS_UNSET+= OPENJPEG
OPTIONS_UNSET+= OPUS
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= RTC
OPTIONS_SET+=   RTCPU
OPTIONS_UNSET+= RTMP
OPTIONS_UNSET+= SCHROEDINGER
OPTIONS_UNSET+= SDL
OPTIONS_UNSET+= SKINS
OPTIONS_UNSET+= SMB
OPTIONS_UNSET+= SNDIO
OPTIONS_UNSET+= SPEEX
OPTIONS_UNSET+= SVGALIB
OPTIONS_UNSET+= V4L
OPTIONS_UNSET+= VDPAU
OPTIONS_UNSET+= VPX
OPTIONS_SET+=   X11
OPTIONS_SET+=   X11DGA
OPTIONS_SET+=   X11VM
OPTIONS_SET+=   XINERAMA
OPTIONS_SET+=   XVIDEO
OPTIONS_SET+=   XVMC
.endif

.if ${.CURDIR:M/usr/ports/multimedia/qmmp-qt5}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= DOXYGEN
OPTIONS_UNSET+= GNOMEHOTKEY
OPTIONS_SET+=   HOTKEY
OPTIONS_UNSET+= KDENOTIFY
OPTIONS_SET+=   NOTIFIER
OPTIONS_SET+=   QMMP_DIALOG
OPTIONS_SET+=   QSUI
OPTIONS_SET+=   SKINNEDUI
OPTIONS_SET+=   STATICON
OPTIONS_SET+=   TWOPANELDIALOG
OPTIONS_UNSET+= CDDA
OPTIONS_SET+=   FAAD
OPTIONS_SET+=   FFMPEG
OPTIONS_SET+=   FLAC
OPTIONS_UNSET+= GME
OPTIONS_SET+=   MAD
OPTIONS_UNSET+= MODPLUG
OPTIONS_UNSET+= MUSEPACK
OPTIONS_SET+=   OPUS
OPTIONS_SET+=   SNDFILE
OPTIONS_SET+=   VORBIS
OPTIONS_SET+=   WAVPACK
OPTIONS_SET+=   WILDMIDI
OPTIONS_SET+=   BS2B
OPTIONS_SET+=   CROSSFADE
OPTIONS_SET+=   LADSPA
OPTIONS_SET+=   SOXR
OPTIONS_SET+=   STEREO
OPTIONS_SET+=   ANALYZER
OPTIONS_SET+=   PROJECTM
OPTIONS_SET+=   ALSA
OPTIONS_UNSET+= JACK
OPTIONS_SET+=   NULLOUT
OPTIONS_UNSET+= OSS
OPTIONS_UNSET+= OSS4
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_SET+=   QTMULTIMEDIA
OPTIONS_SET+=   COPYPASTE
OPTIONS_SET+=   DIR_ASSOC
OPTIONS_SET+=   FILEOPS
OPTIONS_UNSET+= HAL
OPTIONS_UNSET+= TRACKCHANGE
OPTIONS_UNSET+= UDISKS2
OPTIONS_UNSET+= COVER
OPTIONS_UNSET+= CURL
OPTIONS_UNSET+= LYRICS
OPTIONS_UNSET+= MMS
OPTIONS_UNSET+= SB
OPTIONS_UNSET+= SCROBBLER
OPTIONS_UNSET+= CONVERTER
OPTIONS_SET+=   CUE
OPTIONS_SET+=   ENCA
OPTIONS_UNSET+= MPLAYER
OPTIONS_UNSET+= MPRIS
OPTIONS_SET+=   RGSCAN
OPTIONS_SET+=   SID
.endif

.if ${.CURDIR:M/usr/ports/multimedia/qt5-multimedia}
OPTIONS_UNSET+= AUDIOPLUGIN
OPTIONS_SET+=   GSTREAMER
OPTIONS_UNSET+= OPENAL
OPTIONS_UNSET+= ALSA
OPTIONS_UNSET+= PULSEAUDIO
.endif

.if ${.CURDIR:M/usr/ports/multimedia/smplayer}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
.endif

.if ${.CURDIR:M/usr/ports/multimedia/xvid}
OPTIONS_UNSET+= OPTIMIZED_CFLAGS
.endif

.if ${.CURDIR:M/usr/ports/net/dhcpcd}
WITH_DEBUG= yes
.endif

.if ${.CURDIR:M/usr/ports/net/gtk-vnc}
OPTIONS_UNSET+= PULSEAUDIO
.endif

.if ${.CURDIR:M/usr/ports/net/miniupnpd}
OPTIONS_UNSET+= CHECK_PORTINUSE
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= LEASEFILE
OPTIONS_SET+=   PF_FILTER_RULES
OPTIONS_UNSET+= UPNP_IGDV2
OPTIONS_UNSET+= UPNP_STRICT
WITH_DEBUG= yes
.endif

.if ${.CURDIR:M/usr/ports/net/mpd5}
OPTIONS_UNSET+= NG_IPACCT
WITH_DEBUG= yes
.endif

.if ${.CURDIR:M/usr/ports/net/openldap24-client}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= FETCH
OPTIONS_UNSET+= GSSAPI
.endif

.if ${.CURDIR:M/usr/ports/net/samba46}
OPTIONS_UNSET+= ADS
OPTIONS_UNSET+= AD_DC
OPTIONS_UNSET+= CUPS
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DEVELOPER
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXP_MODULES
OPTIONS_UNSET+= FAM
OPTIONS_UNSET+= LDAP
OPTIONS_UNSET+= MANPAGES
OPTIONS_UNSET+= NTVFS
OPTIONS_UNSET+= QUOTAS
OPTIONS_UNSET+= SYSLOG
OPTIONS_UNSET+= UTMP
OPTIONS_UNSET+= NSUPDATE
OPTIONS_UNSET+= BIND99
OPTIONS_UNSET+= BIND910
OPTIONS_UNSET+= BIND911
OPTIONS_UNSET+= AVAHI
OPTIONS_UNSET+= MDNSRESPONDER
.endif

.if ${.CURDIR:M/usr/ports/net/vde2}
OPTIONS_SET+=   PYTHON
.endif

.if ${.CURDIR:M/usr/ports/net/wireshark-qt5}
OPTIONS_UNSET+= ANDROIDDUMP
OPTIONS_UNSET+= CARES
OPTIONS_UNSET+= DECRYPT
OPTIONS_UNSET+= GEOIP
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= LUA
OPTIONS_UNSET+= RTP
OPTIONS_UNSET+= SNMP
OPTIONS_UNSET+= KRB_BASE
OPTIONS_UNSET+= KRB_HEIMDAL
OPTIONS_UNSET+= KRB_MIT
OPTIONS_SET+=   KRB_NONE
OPTIONS_SET+=   QT5
.endif

.if ${.CURDIR:M/usr/ports/net-p2p/transmission-cli}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= LITE
OPTIONS_SET+=   OPENSSL
OPTIONS_UNSET+= POLARSSL
OPTIONS_UNSET+= WOLFSSL
.endif

.if ${.CURDIR:M/usr/ports/net-p2p/transmission-daemon}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= LITE
OPTIONS_SET+=   OPENSSL
OPTIONS_UNSET+= POLARSSL
OPTIONS_UNSET+= WOLFSSL
.endif

.if ${.CURDIR:M/usr/ports/ports-mgmt/portmaster}
OPTIONS_UNSET+= BASH
OPTIONS_UNSET+= ZSH
.endif

.if ${.CURDIR:M/usr/ports/print/cups}
OPTIONS_UNSET+= AVAHI
OPTIONS_SET+=   DBUS
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= GNUTLS
OPTIONS_UNSET+= IPPTOOL
OPTIONS_SET+=   LIBPAPER
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= X11
.endif

.if ${.CURDIR:M/usr/ports/print/fontforge}
OPTIONS_UNSET+= CAIRO
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= FREETYPE
OPTIONS_UNSET+= PYTHON
.endif

.if ${.CURDIR:M/usr/ports/print/freetype2}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= LCD_FILTERING
OPTIONS_UNSET+= LONG_PCF_NAMES
OPTIONS_UNSET+= PNG
OPTIONS_UNSET+= TABLE_VALIDATION
OPTIONS_UNSET+= V38
OPTIONS_SET+=   V40
OPTIONS_UNSET+= FIX_SIZE_METRICS
OPTIONS_UNSET+= TT_SIZE_METRICS
.endif

.if ${.CURDIR:M/usr/ports/print/qt5-printsupport}
OPTIONS_SET+=   CUPS
.endif

.if ${.CURDIR:M/usr/ports/science/hdf5}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= FORTRAN
OPTIONS_SET+=   SZIP
.endif

.if ${.CURDIR:M/usr/ports/security/ca_root_nss}
OPTIONS_UNSET+= ETCSYMLINK
.endif

.if ${.CURDIR:M/usr/ports/security/cyrus-sasl2}
OPTIONS_UNSET+= ALWAYSTRUE
OPTIONS_SET+=   AUTHDAEMOND
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= KEEP_DB_OPEN
OPTIONS_SET+=   OBSOLETE_CRAM_ATTR
OPTIONS_UNSET+= MYSQL
OPTIONS_UNSET+= PGSQL
OPTIONS_SET+=   ANONYMOUS
OPTIONS_SET+=   CRAM
OPTIONS_SET+=   DIGEST
OPTIONS_SET+=   LOGIN
OPTIONS_SET+=   NTLM
OPTIONS_SET+=   OTP
OPTIONS_SET+=   PLAIN
OPTIONS_SET+=   SCRAM
OPTIONS_SET+=   BDB1
OPTIONS_UNSET+= BDB
OPTIONS_UNSET+= GDBM
OPTIONS_UNSET+= SQLITE2
OPTIONS_UNSET+= SQLITE3
.endif

.if ${.CURDIR:M/usr/ports/security/gnutls}
OPTIONS_UNSET+= DANE
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_SET+=   IDN
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= P11KIT
OPTIONS_UNSET+= TPM
OPTIONS_SET+=   ZLIB
.endif

.if ${.CURDIR:M/usr/ports/security/libssh2}
OPTIONS_UNSET+= GCRYPT
OPTIONS_UNSET+= TRACE
OPTIONS_SET+=   ZLIB
.endif

.if ${.CURDIR:M/usr/ports/security/nss}
OPTIONS_UNSET+= DEBUG
.endif

.if ${.CURDIR:M/usr/ports/security/openvpn}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EASYRSA
OPTIONS_UNSET+= EXAMPLES
OPTIONS_SET+=   LZ4
OPTIONS_UNSET+= PKCS11
OPTIONS_UNSET+= SMALL
OPTIONS_UNSET+= TEST
OPTIONS_UNSET+= TUNNELBLICK
OPTIONS_UNSET+= X509ALTUSERNAME
OPTIONS_SET+=   OPENSSL
OPTIONS_UNSET+= MBEDTLS
WITH_DEBUG= yes
.endif

.if ${.CURDIR:M/usr/ports/shells/bash}
OPTIONS_SET+=   COLONBREAKSWORDS
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= FDESCFS
OPTIONS_UNSET+= HELP
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= STATIC
OPTIONS_UNSET+= SYSBASHRC
OPTIONS_UNSET+= SYSLOG
.endif

.if ${.CURDIR:M/usr/ports/shells/zsh}
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= ETCDIR
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= GDBM
OPTIONS_UNSET+= MEM
OPTIONS_UNSET+= PCRE
OPTIONS_UNSET+= STATIC
WITH_DEBUG= yes
.endif

.if ${.CURDIR:M/usr/ports/sysutils/cdrtools}
OPTIONS_UNSET+= CDDA2MP3
OPTIONS_UNSET+= CDDA2OGG
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= RSCSI
.endif

.if ${.CURDIR:M/usr/ports/sysutils/polkit}
OPTIONS_UNSET+= MANPAGES
.endif

.if ${.CURDIR:M/usr/ports/sysutils/tmux}
OPTIONS_UNSET+= BACKSPACE
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= LIBEVENT_STATIC
.endif

.if ${.CURDIR:M/usr/ports/textproc/aspell}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= ISPELL
OPTIONS_SET+=   NLS
.endif

.if ${.CURDIR:M/usr/ports/textproc/docbook-xsl}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= ECLIPSE
OPTIONS_SET+=   EPUB
OPTIONS_SET+=   EXTENSIONS
OPTIONS_SET+=   HIGHLIGHTING
OPTIONS_SET+=   HTMLHELP
OPTIONS_UNSET+= JAVAHELP
OPTIONS_SET+=   PROFILING
OPTIONS_SET+=   ROUNDTRIP
OPTIONS_UNSET+= SLIDES
OPTIONS_SET+=   TEMPLATE
OPTIONS_SET+=   TESTS
OPTIONS_SET+=   TOOLS
OPTIONS_SET+=   WEBSITE
OPTIONS_SET+=   XHTML11
.endif

.if ${.CURDIR:M/usr/ports/textproc/enchant}
OPTIONS_UNSET+= ASPELL
OPTIONS_UNSET+= HSPELL
OPTIONS_SET+=   HUNSPELL
OPTIONS_UNSET+= ISPELL
OPTIONS_UNSET+= ZEMBEREK
.endif

.if ${.CURDIR:M/usr/ports/textproc/librevenge}
OPTIONS_UNSET+= DOXYGEN
.endif

.if ${.CURDIR:M/usr/ports/textproc/libxml2}
OPTIONS_UNSET+= MEM_DEBUG
OPTIONS_SET+=   SCHEMA
OPTIONS_SET+=   THREADS
OPTIONS_UNSET+= THREAD_ALLOC
OPTIONS_SET+=   VALID
OPTIONS_UNSET+= XMLLINT_HIST
.endif

.if ${.CURDIR:M/usr/ports/textproc/libxslt}
OPTIONS_SET+=   CRYPTO
OPTIONS_UNSET+= MEM_DEBUG
.endif

.if ${.CURDIR:M/usr/ports/textproc/py-docutils}
OPTIONS_UNSET+= PYGMENTS
.endif

.if ${.CURDIR:M/usr/ports/textproc/py-snowballstemmer}
OPTIONS_SET+=   PYSTEMMER
.endif


.if ${.CURDIR:M/usr/ports/textproc/rasqal}
OPTIONS_UNSET+= GMP
OPTIONS_UNSET+= MPFR
.endif

.if ${.CURDIR:M/usr/ports/textproc/redland}
OPTIONS_UNSET+= BDB
OPTIONS_UNSET+= THREADS
.endif

.if ${.CURDIR:M/usr/ports/textproc/tinyxml}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= PROFILE
.endif

.if ${.CURDIR:M/usr/ports/www/firefox}
OPTIONS_SET+=   BUNDLED_CAIRO
OPTIONS_UNSET+= CANBERRA
OPTIONS_SET+=   DBUS
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DTRACE
OPTIONS_SET+=   FFMPEG
OPTIONS_UNSET+= GCONF
OPTIONS_UNSET+= GNOMEUI
OPTIONS_UNSET+= INTEGER_SAMPLES
OPTIONS_UNSET+= LIBPROXY
OPTIONS_SET+=   OPTIMIZED_CFLAGS
OPTIONS_UNSET+= PGO
OPTIONS_UNSET+= PROFILE
OPTIONS_SET+=   RUST
OPTIONS_UNSET+= TEST
OPTIONS_SET+=   ALSA
OPTIONS_UNSET+= JACK
OPTIONS_UNSET+= PULSEAUDIO
OPTIONS_UNSET+= SNDIO
OPTIONS_UNSET+= GTK2
OPTIONS_SET+=   GTK3
.endif

.if ${.CURDIR:M/usr/ports/www/nginx}
OPTIONS_SET+=   DSO
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= DEBUGLOG
OPTIONS_UNSET+= FILE_AIO
OPTIONS_SET+=   IPV6
OPTIONS_UNSET+= GOOGLE_PERFTOOLS
OPTIONS_SET+=   HTTP
OPTIONS_UNSET+= HTTP_ADDITION
OPTIONS_UNSET+= HTTP_AUTH_REQ
OPTIONS_SET+=   HTTP_CACHE
OPTIONS_SET+=   HTTP_DAV
OPTIONS_UNSET+= HTTP_FLV
OPTIONS_UNSET+= HTTP_GEOIP
OPTIONS_UNSET+= HTTP_GZIP_STATIC
OPTIONS_UNSET+= HTTP_GUNZIP_FILTER
OPTIONS_UNSET+= HTTP_IMAGE_FILTER
OPTIONS_UNSET+= HTTP_MP4
OPTIONS_UNSET+= HTTP_PERL
OPTIONS_UNSET+= HTTP_RANDOM_INDEX
OPTIONS_UNSET+= HTTP_REALIP
OPTIONS_SET+=   HTTP_REWRITE
OPTIONS_UNSET+= HTTP_SECURE_LINK
OPTIONS_UNSET+= HTTP_SLICE
OPTIONS_SET+=   HTTP_SSL
OPTIONS_SET+=   HTTP_STATUS
OPTIONS_UNSET+= HTTP_SUB
OPTIONS_UNSET+= HTTP_XSLT
OPTIONS_UNSET+= MAIL
OPTIONS_UNSET+= MAIL_IMAP
OPTIONS_UNSET+= MAIL_POP3
OPTIONS_UNSET+= MAIL_SMTP
OPTIONS_UNSET+= MAIL_SSL
OPTIONS_SET+=   HTTPV2
OPTIONS_UNSET+= NJS
OPTIONS_UNSET+= STREAM
OPTIONS_UNSET+= STREAM_SSL
OPTIONS_UNSET+= STREAM_SSL_PREREAD
OPTIONS_UNSET+= THREADS
OPTIONS_SET+=   WWW
OPTIONS_UNSET+= AJP
OPTIONS_UNSET+= AWS_AUTH
OPTIONS_UNSET+= CACHE_PURGE
OPTIONS_UNSET+= CLOJURE
OPTIONS_UNSET+= CT
OPTIONS_UNSET+= ECHO
OPTIONS_UNSET+= FASTDFS
OPTIONS_UNSET+= HEADERS_MORE
OPTIONS_UNSET+= HTTP_ACCEPT_LANGUAGE
OPTIONS_UNSET+= HTTP_ACCESSKEY
OPTIONS_UNSET+= HTTP_AUTH_DIGEST
OPTIONS_UNSET+= HTTP_AUTH_KRB5
OPTIONS_UNSET+= HTTP_AUTH_LDAP
OPTIONS_UNSET+= HTTP_AUTH_PAM
OPTIONS_UNSET+= HTTP_DAV_EXT
OPTIONS_UNSET+= HTTP_EVAL
OPTIONS_UNSET+= HTTP_FANCYINDEX
OPTIONS_UNSET+= HTTP_FOOTER
OPTIONS_UNSET+= HTTP_GEOIP2
OPTIONS_UNSET+= HTTP_JSON_STATUS
OPTIONS_UNSET+= HTTP_MOGILEFS
OPTIONS_UNSET+= HTTP_MP4_H264
OPTIONS_UNSET+= HTTP_NOTICE
OPTIONS_UNSET+= HTTP_PUSH
OPTIONS_UNSET+= HTTP_PUSH_STREAM
OPTIONS_UNSET+= HTTP_REDIS
OPTIONS_UNSET+= HTTP_RESPONSE
OPTIONS_UNSET+= HTTP_SUBS_FILTER
OPTIONS_UNSET+= HTTP_TARANTOOL
OPTIONS_SET+=   HTTP_UPLOAD
OPTIONS_UNSET+= HTTP_UPLOAD_PROGRESS
OPTIONS_UNSET+= HTTP_UPSTREAM_CHECK
OPTIONS_UNSET+= HTTP_UPSTREAM_FAIR
OPTIONS_UNSET+= HTTP_UPSTREAM_STICKY
OPTIONS_UNSET+= HTTP_VIDEO_THUMBEXTRACTOR
OPTIONS_UNSET+= HTTP_ZIP
OPTIONS_UNSET+= ARRAYVAR
OPTIONS_UNSET+= BROTLI
OPTIONS_UNSET+= DRIZZLE
OPTIONS_UNSET+= DYNAMIC_UPSTREAM
OPTIONS_UNSET+= ENCRYPTSESSION
OPTIONS_UNSET+= FORMINPUT
OPTIONS_UNSET+= GRIDFS
OPTIONS_UNSET+= ICONV
OPTIONS_UNSET+= LET
OPTIONS_UNSET+= LUA
OPTIONS_UNSET+= MEMC
OPTIONS_UNSET+= MODSECURITY
OPTIONS_UNSET+= MODSECURITY_DEVEL
OPTIONS_UNSET+= NAXSI
OPTIONS_UNSET+= PASSENGER
OPTIONS_UNSET+= POSTGRES
OPTIONS_UNSET+= RDS_CSV
OPTIONS_UNSET+= RDS_JSON
OPTIONS_UNSET+= REDIS2
OPTIONS_UNSET+= RTMP
OPTIONS_UNSET+= SET_MISC
OPTIONS_UNSET+= SFLOW
OPTIONS_UNSET+= SHIBBOLETH
OPTIONS_UNSET+= SLOWFS_CACHE
OPTIONS_UNSET+= SMALL_LIGHT
OPTIONS_UNSET+= SRCACHE
OPTIONS_UNSET+= STATSD
OPTIONS_UNSET+= UDPLOG
OPTIONS_UNSET+= X11
OPTIONS_UNSET+= XRID_HEADER
OPTIONS_UNSET+= XSS
.endif

.if ${.CURDIR:M/usr/ports/www/qt4-webkit}
OPTIONS_SET+=   GSTREAMER
.endif

.if ${.CURDIR:M/usr/ports/www/qt5-webkit}
OPTIONS_SET+=   GSTREAMER
.endif

.if ${.CURDIR:M/usr/ports/www/qupzilla-qt5}
OPTIONS_SET+=   DBUS
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= GNOMEKEYRING
OPTIONS_UNSET+= KWALLET
.endif

.if ${.CURDIR:M/usr/ports/www/serf}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= MIT
OPTIONS_UNSET+= HEIMDAL
OPTIONS_UNSET+= HEIMDAL_BASE
.endif

.if ${.CURDIR:M/usr/ports/www/thttpd}
OPTIONS_SET+=   FLUSH_LOG
OPTIONS_UNSET+= HTACCESS
OPTIONS_UNSET+= INDEXES
OPTIONS_UNSET+= LOG_UNKNOWN
OPTIONS_SET+=   REAL_IP
OPTIONS_SET+=   SENDFILE
OPTIONS_SET+=   STATS
.endif

.if ${.CURDIR:M/usr/ports/www/transmission-web}
OPTIONS_UNSET+= LITE
.endif

.if ${.CURDIR:M/usr/ports/www/youtube_dl}
OPTIONS_UNSET+= BASH
OPTIONS_UNSET+= FFMPEG
OPTIONS_UNSET+= FISH
OPTIONS_UNSET+= RTMPDUMP
OPTIONS_UNSET+= ZSH
.endif

.if ${.CURDIR:M/usr/ports/x11/babl}
OPTIONS_UNSET+= SIMD
.endif

.if ${.CURDIR:M/usr/ports/x11/keybinder}
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= GTKDOC
OPTIONS_UNSET+= LUA
OPTIONS_SET+=   PYTHON
.endif

.if ${.CURDIR:M/usr/ports/x11/pixman}
OPTIONS_SET+=   SIMD
.endif

.if ${.CURDIR:M/usr/ports/x11/terminator}
OPTIONS_UNSET+= GCONF
OPTIONS_UNSET+= NLS
.endif

.if ${.CURDIR:M/usr/ports/x11/xkeyboard-config}
OPTIONS_UNSET+= ETERSOFT
.endif

.if ${.CURDIR:M/usr/ports/x11/xterm}
OPTIONS_SET+=   256COLOR
OPTIONS_UNSET+= DABBREV
OPTIONS_UNSET+= DECTERM
OPTIONS_UNSET+= GNOME
OPTIONS_UNSET+= LOGGING
OPTIONS_UNSET+= LUIT
OPTIONS_UNSET+= PCRE
OPTIONS_UNSET+= REGIS
OPTIONS_UNSET+= SCRNDUMP
OPTIONS_UNSET+= SIXEL
OPTIONS_SET+=   WCHAR
OPTIONS_UNSET+= XINERAMA
OPTIONS_UNSET+= XAW3D
OPTIONS_UNSET+= XAW3DXFT
OPTIONS_UNSET+= NEXTAW
.endif

.if ${.CURDIR:M/usr/ports/x11-fonts/dejavu}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= LINKS
.endif

.if ${.CURDIR:M/usr/ports/x11-fonts/fontconfig}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= HINTING_NONE
OPTIONS_SET+=   HINTING_SLIGHT
OPTIONS_UNSET+= HINTING_MEDIUM
OPTIONS_UNSET+= HINTING_FULL
.endif

.if ${.CURDIR:M/usr/ports/x11-servers/xorg-server}
OPTIONS_SET+=   AIGLX
OPTIONS_SET+=   SUID
OPTIONS_UNSET+= HAL
OPTIONS_SET+=   DEVD
.endif

.if ${.CURDIR:M/usr/ports/x11-themes/gtk-engines2}
OPTIONS_UNSET+= ANIMATION
.endif

.if ${.CURDIR:M/usr/ports/x11-toolkits/gtk20}
OPTIONS_UNSET+= CUPS
OPTIONS_UNSET+= DEBUG
.endif

.if ${.CURDIR:M/usr/ports/x11-toolkits/gtk30}
OPTIONS_SET+=   BROADWAY
OPTIONS_UNSET+= CLOUDPRINT
OPTIONS_SET+=   COLORD
OPTIONS_UNSET+= CUPS
OPTIONS_UNSET+= DEBUG
OPTIONS_UNSET+= TYPEAHEAD
.endif

.if ${.CURDIR:M/usr/ports/x11-toolkits/pango}
OPTIONS_SET+=   X11
.endif

.if ${.CURDIR:M/usr/ports/x11-toolkits/py-gtk2}
OPTIONS_UNSET+= DOCS
OPTIONS_UNSET+= EXAMPLES
OPTIONS_UNSET+= NUMPY
.endif

.if ${.CURDIR:M/usr/ports/x11-toolkits/qt5-widgets}
OPTIONS_UNSET+= GTK3
.endif

.if ${.CURDIR:M/usr/ports/x11-toolkits/wxgtk30}
OPTIONS_SET+=   GSTREAMER
OPTIONS_UNSET+= MSPACK
OPTIONS_UNSET+= NLS
OPTIONS_UNSET+= WEBKIT
.endif

.if ${.CURDIR:M/usr/ports/x11-wm/compton}
OPTIONS_SET+=   CONFIG
OPTIONS_SET+=   DBUS
OPTIONS_UNSET+= DOCS
OPTIONS_SET+=   DRM
OPTIONS_SET+=   OPENGL
OPTIONS_SET+=   PCRE
OPTIONS_SET+=   XINERAMA
.endif

.if ${.CURDIR:M/usr/ports/x11-wm/openbox}
OPTIONS_SET+=   IMLIB
OPTIONS_UNSET+= NLS
OPTIONS_SET+=   NOTIFY
OPTIONS_SET+=   PANGO
OPTIONS_SET+=   SVG
.endif
